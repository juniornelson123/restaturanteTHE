
  <header id="gtco-header" class="gtco-cover gtco-cover-sm" role="banner" style="background-image: url(<%= asset_path 'img_rest.jpg' %>)" data-stellar-background-ratio="0.5">
    <div class="overlay"></div>
    <div class="gtco-container">
      <div class="row">
        <div class="col-md-12 col-md-offset-0 text-center">
          

          <div class="row row-mt-15em">
            <div class="col-md-12 mt-text animate-box" data-animate-effect="fadeInUp">
              <h1 class="cursive-font">Cardapio!</h1> 
            </div>
            
          </div>
              
          
        </div>
      </div>
    </div>
  </header>

<nav class="main-nav-outer" id="test"><!--main-nav-start-->
  <div class="container">
        <ul class="main-nav">
        <% @categories.each do |c| %>
            <li><a href="?category=<%= c.id %>" data-remote="true"><%= c.name %></a></li>
        <% end %>  
            <li><a href="?category=tudo" data-remote="true">tudo</a></li>
        </ul>
    </div>
</nav><!--main-nav-end-->



  <div class="gtco-section">
    <div class="gtco-container">
          <div class='loading'></div>
            <div id='books_container'>
    <div class="row">
        <div class="col-md-8 col-md-offset-2 text-center gtco-heading">
          <h2 class="cursive-font primary-color">Todos os pratos</h2>
            <div id="add"></div>
        </div>
      </div>
  
      <div class="row">
              <div class="row">
                <div class="col-md-1 col-md-offset-11 text-center gtco-heading">
                  <%= paginate @products ,remote: true%>
                </div>
              </div>
            <% @products.each do |product| %>
       
            <div class="col-lg-4 col-md-4 col-sm-6">
              <a href="/admin/establishments/<%= params[:establishment_id]%>/comandas?product_id=<%= product.id%>" class="fh5co-card-item " data-method="post" data-remote="true">
                <figure>
                  <div class="overlay"><i class="ti-plus"></i></div>
                <%= image_tag product.image_url, :class => 'center-block img-responsive' if product.image? %>
                </figure>
                <div class="fh5co-text">
                  <h2><%= product.name %></h2>
                  <p><%= product.description %></p>
                  <p><span class="price cursive-font">R$ <%= (number_with_precision(product.price, :precision => 2) || 0)%></span></p>
                </div>
                <!--<p><%= link_to "Adicionar",admin_establishment_comandas_path(product_id: product.id, establishment_id: params[:establishment_id]) , method: :post, remote: true%></p>-->
              </a>

                    
        </div>
        
    <% end %>

      <div class="row">
        <div class="col-md-1 col-md-offset-11 text-center gtco-heading">
          <%= paginate @products,remote: true%>
        </div>
      </div>
    </div>

      </div>
    </div>
  </div>

 